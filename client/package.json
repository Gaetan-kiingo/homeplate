{
  "name": "homeplate-client",
  "version": "1.0.0",
  "private": true,
  "description": "Homeplate v1.0 responsive React WEB client (SRS §2.1.2 — web, not React Native; WA-9). Own npm package: the root package's `npm test` contract (backend Jest suite) is untouched.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^24.1.3",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}

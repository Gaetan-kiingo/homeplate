# ADR-007: Moderation LLM provider — Google Gemini free tier behind the provider-agnostic adapter

- **Status:** **Accepted — fully ratified.** The free-tier data-use review, previously the one open action, was signed on 2026-08-18 (option (a) + (b)); the SPMP §7.4 countersignature is outstanding.
- **Date:** 2026-08-12
- **Deciders:** Gaetan Rieben (decided 2026-08-12; data-use review signed 2026-08-18, countersignature by Nam Tran outstanding)
- **Related requirements:** FR-08 (moderation), NFR-09 (degradation), NFR-10 (moderation accuracy), NFR-13 (data protection), SRS §2.4 (free-tier constraint)
- **Open action — free-tier data-use review (ST-06): CLOSED 2026-08-18.** The *evidence* was recorded 2026-08-14 in [`docs/adr007-data-use-review.md`](../docs/adr007-data-use-review.md) and re-fetched from the live terms page on 2026-08-18 (effective date still 2026-03-23; findings F1–F4 confirmed verbatim): the free tier reserves submitted content for product improvement and human review, and instructs callers not to submit personal information. The *ratification* has now happened — **Gaetan Rieben signed §7 on 2026-08-18, ratifying option (a) + (b)**; the SPMP §7.4 countersignature by Nam Tran is still outstanding and is tracked there. The ratified rule binds wave 4: **only non-personal content may reach the live provider.** In v1.0 that gate passes for everything, because the prototype is exercised solely with synthetic, team-authored data and holds no real user content at all — so U4-MODERATION builds and exercises the full FR-08 confidence-routing path, with the human Moderator queue as the ADR-002 fallback, and public content still never publishes unreviewed. Real user content appears only if the project is taken to market, and that is a **paid-tier + re-signature** event under that file's §7.2 re-review triggers, not a configuration change. U4-MODERATION must still gate live classification on the *signature and the content*, never on the mere existence of the review file.

## Context
ADR-002 fixed the moderation approach — deterministic pre-filter, then a hosted LLM returning a structured `{category, confidence}` result — but deliberately left the runtime configuration open, consistent with SRS §2.4's separation of fixed technology constraints from open decisions. Build planning surfaced the gap as a hard conflict: SRS §2.4 requires that any API use stay within its free-tier rate limits, and the SPMP (§1.1.2, §5.1.3) states there is no project budget, yet the moderation provider assumed during planning has no free tier. FR-08 cannot be satisfied by the pre-filter alone — the requirement names an automated moderation flow with confidence-based routing to human review — so dropping the LLM stage is not available.

## Decision
The moderation LLM stage will call the **Google Gemini API free tier**, using a Google AI Studio key held by the team. The call sits behind the provider-agnostic adapter already specified in the build plan (`LLM_MODERATION_BASE_URL`, `LLM_MODERATION_API_KEY`, `MODERATION_MODEL`), so the provider is one environment change rather than a code change. The concrete model id is pinned in configuration at implementation time against whatever model the free tier currently offers, and recorded alongside the IT-03 results so a measurement can be tied to the model that produced it.

CI and the automated test suite run against a deterministic mock adapter; only the IT-03 measurement run calls the live API. A moderation-provider outage or quota exhaustion is handled exactly as ADR-002 requires — public content stays pending, never publishes unreviewed.

## Alternatives considered
- **A paid API tier** — rejected: there is no budget (SPMP §5.1.3), and SRS §2.4 makes free-tier operation a constraint rather than a preference.
- **A self-hosted small open-weights model** — rejected: it moves the cost from money to hardware and team hours, and the SPMP's ≈8-week window with three people (§5.1.1) has no room for model serving.
- **Pre-filter only, no LLM stage** — rejected: FR-08 requires confidence-based routing to human review, which a blocklist cannot produce, and NFR-10 would have nothing to measure.

## Consequences
- **Positive:** The free-tier constraint in SRS §2.4 is satisfied without weakening FR-08. Because the adapter is provider-agnostic, a later move to a paid or different provider costs one environment variable, not a rewrite.
- **Negative — privacy, and the most important consequence of this decision:** free tiers of consumer AI APIs commonly reserve the right to use submitted content for product improvement and human review, on terms that differ from paid tiers. The moderation pipeline scans private host–guest messages, and the SRS §3.4 PII register already lists the LLM API as an external recipient of message content. The terms were read on 2026-08-14 and re-read on 2026-08-18 (`docs/adr007-data-use-review.md`): the free tier does permit retention, training use and human review of submitted content, and explicitly says not to submit personal information — so option (c) paid tier is closed by SRS §2.4 / SPMP §5.1.3 for a project with no budget, and (b) pseudonymization alone is insufficient because the message body *is* the personal content. **The team ratified (a) + (b) on 2026-08-18** (Gaetan Rieben; countersignature outstanding): only non-personal content may reach the live provider. This costs v1.0 nothing, because v1.0 is a course prototype exercised solely with synthetic, team-authored data and holds no real user content — the gate passes for every item it can contain. It does NOT weaken ADR-002: the "would have called the LLM" path still routes to the human Moderator queue whenever the content gate fails, and public content still never publishes unreviewed. Taking the product to market introduces real user content and therefore requires the paid tier and a fresh signature *before* any such content is sent.
- **Negative — dependency concentration:** Google now serves both discovery (ADR-005, Maps/Places) and moderation. A Google-side outage or a billing/quota problem degrades two subsystems at once, which widens the blast radius RT-01 must exercise under NFR-09.
- **Negative — throughput:** free-tier rate limits constrain IT-03's ≥200-item evaluation run, which will need batching and backoff and is not safe to run in CI on every commit.
- **Neutral / follow-ups:** IT-03 (SRS §4.2) remains the gate on NFR-10. Per ADR-002, a failed bound is answered by measurement and prompt work first, not by reaching for a more complex classifier.

## AI assistance & provenance
The conflict between SRS §2.4's free-tier constraint and the moderation provider was surfaced by the AI-assisted build-planning run on 2026-08-11 (see `docs/_generated/build-plan.md`, open question 5), which flagged it rather than resolving it. The choice of Gemini's free tier was made by the team on cost grounds. This record was drafted by Claude Code from that decision; the privacy consequence above was an open action item until 2026-08-18. On 2026-08-14 the free-tier terms were read and the finding recorded in `docs/adr007-data-use-review.md`, discharging the *research* half of the clause; on **2026-08-18 Gaetan Rieben ratified option (a) + (b)**, discharging the *ratification* half. Both halves are logged there, including a provenance note stating that the sign-off block records a decision the named human took in session and that the agent transcribed at their instruction — an agent may not choose or invent it. The research and its analysis are AI-assisted, so SPMP §5.4/§7.4 applies to them as to any AI-assisted artifact: **the peer countersignature by Nam Tran is still outstanding** and must be recorded in that file's §7 table and in the weekly stand-up report before the clause is presented as fully peer-reviewed. The restriction itself never depended on the signature — unsigned failed closed — and it does not lapse now that one exists; what changed is that the team owns it.
